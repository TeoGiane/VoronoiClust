% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/generate_multiview_data.R
\name{generate_multiview_data}
\alias{generate_multiview_data}
\title{Generate Multi-View Data}
\usage{
generate_multiview_data(
  n_obs,
  n_vars = 1,
  n_clusters = 2,
  n_views = 1,
  dirichlet_conc = 1,
  simplex_radius = 1,
  noise_sd = 0.1,
  agreement_rate = 0,
  random_seed = NULL,
  base_clusters = NULL
)
}
\arguments{
\item{n_obs}{Integer. The total number of observations (data points) to generate.}

\item{n_vars}{Integer. The number of variables (size of each data point) in each view [Default: 1].}

\item{n_clusters}{Integer. The number of latent clusters [Default: 2].}

\item{n_views}{Integer. The number of data views to generate [Default: 1].}

\item{dirichlet_conc}{Numeric. The concentration parameter for the Dirichlet distribution used to generate base cluster probabilities [Default: 1.0].}

\item{simplex_radius}{Numeric. The distance of each cluster center from the origin along orthogonal axes, defining the vertices of a regular simplex. Controls the baseline separation between clusters (distance between centres is \eqn{r\sqrt{2}} [Default: 1.0].}

\item{noise_sd}{Numeric vector or scalar. The standard deviation of the Gaussian noise added to the points. If a scalar is provided, the same noise level is applied to all views. If a vector, it must have length `n_views` [Default: 0.1].}

\item{agreement_rate}{Numeric vector or scalar. The exact fraction (between 0 and 1) of observations in views 2 through `n_views` that retain their cluster assignment from the first view. If a scalar is provided, the same agreement rate is applied to all views. If a vector, it must have length `n_views - 1`. Controls the correlation/agreement between views [Default: 0.0].}

\item{random_seed}{Integer. Optional seed for reproducibility [Default: NULL].}

\item{base_clusters}{Integer vector. Optional pre-specified cluster assignments for the first view. Must have length `n_obs`. If NULL, assignments are generated automatically [Default: NULL].}
}
\value{
A list containing the generated multi-view data and ground truth:
\describe{
  \item{points}{A list of length `n_views`, where each element is an `n_obs` x `n_vars` matrix of data points.}
  \item{clusters}{A list of length `n_views`, where each element is an integer vector of length `n_obs` representing cluster assignments.}
  \item{distances}{A list of length `n_views`, where each element is an `n_obs` x `n_obs` distance matrix.}
  \item{probs}{A numeric vector of length `n_clusters` containing the underlying cluster probabilities.}
  \item{centres}{An `n_clusters` x `n_vars` matrix of the base cluster centres.}
}
}
\description{
Generates synthetic multi-view clustering data with an arbitrary number of views,
configurable noise levels, and controlled cluster agreement across views.
The generation strictly adheres to the procedure of retaining exact fractions
of cluster assignments while permuting the remainder, preserving marginal
cluster proportions across all views.
}
